# portfolio-go-lib

# 概要
本リポジトリはGoのプロジェクトにおける、共通ライブラリになります。

本ライブラリには、テスト用のモックファイルも同梱されております。
各プロジェクトでDIで読み込むことで、テスト時にモックの使用が可能となります。

# 使用方法
各プロジェクトで go get を行なっていただくことで使用が可能となっております。

# メモ欄

タグ一覧確認

```
git tag
```

タグ生成方法

```
git tag -a vX.X.X -m "vX.X.X"
git push origin vX.X.X
```
